const categoryButton = document.getElementById("category-button")
const categoryDialog = document.getElementById("category-dialog")

categoryButton.addEventListener('click', () => categoryDialog.show());