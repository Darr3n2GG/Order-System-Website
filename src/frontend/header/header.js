const nav_guest = document.getElementById("nav-guest");
const nav_registered = document.getElementById("nav-registered");

function showNavGuest() {
    nav_guest.classList.add("show_nav");
}
function hideNavGuest() {
    nav_guest.classList.remove("show_nav");
}

function showNavRegistered() {
    nav_guest.classList.add("show_nav");
}

function hideNavRegistered() {
    nav_guest.classList.remove("show_nav");
}